# 《거울빛 (MIRRORLIGHT)》 게임 구조 및 구현 명세서 v1.1

> v1.1 변경: 수치 감사에서 발견된 원포즈 치팅 봉쇄(걸이 위치·원뿔 수치 조정, 검증 조건 0 신설), 소지 상태 복귀 규칙 명문화, RETRIEVED 역설과 퍼즐 3 충돌 해소, 과거층 거울 원형의 차폐 규칙, 남쪽 레일 지대 비보행 처리.

> 한 문장 요약: **과거는 거울 빛이 닿는 곳에만 존재한다.**
> 플랫폼: 웹 브라우저 (정적 페이지, 서버 없음) · 인원: 1인 · 시점: 1인칭 3D

---

## 0. 이 문서를 읽는 법

이 문서는 두 종류의 독자를 위해 쓰였다.

- **게임을 처음 접하는 사람**은 1장부터 5장까지 읽으면 게임의 의도, 규칙, 그리고 대표 퍼즐 하나의 전체 흐름을 이해할 수 있다. 5장의 워크스루가 이 게임의 모든 규칙을 한 번씩 시연한다.
- **구현을 맡은 코딩 에이전트**는 6장부터 10장까지가 기술 명세다. 다만 6장을 읽기 전에 반드시 3장(거울의 법)과 5장(워크스루)을 먼저 읽어야 한다. 모든 기술 결정은 3장의 규칙을 코드로 옮긴 것이며, 규칙과 코드가 충돌할 때는 규칙이 우선한다.

---

## 1. 게임 개요

### 1.1 무엇을 하는 게임인가

플레이어는 낡은 방 하나에 갇혀 있다. 방에는 바퀴 달린 전신 거울이 하나 있는데, 이 거울은 현재를 비추지 않는다. **10년 전의 이 방**을 비춘다. 거울을 밀고 돌려서 원하는 벽과 바닥에 과거를 비추고, 거울을 들여다보면 의식이 거울 속 분신으로 옮겨 가 과거를 걸을 수 있다. 단, 분신은 거울 빛이 닿는 범위 밖으로는 한 발짝도 나갈 수 없다. 과거에서 바꾼 것은 10년의 세월을 먹은 모습으로 현재에 반영된다. 남쪽 벽에는 들어갈 수 없는 작은 손거울이 하나 더 걸려 있고, 그것은 **멸망 이후의 이 방**을 비춘다. 미래를 읽고, 과거를 바꾸고, 현재에서 수확하여 잠긴 문을 여는 것이 목표다.

### 1.2 플레이어가 느껴야 하는 것

1. **거울은 문이 아니라 손전등이다.** 과거 전체가 존재하는 것이 아니라, 내가 빛을 비춘 조각만 존재한다. 거울을 옮기는 손이 곧 과거의 지도를 그리는 손이다.
2. **시간은 배달부다.** 물건은 거울면을 통과할 수 없다. 과거의 물건을 현재로 가져오는 유일한 방법은, 10년을 견딜 곳에 숨기고 현재에서 꺼내는 것이다.
3. **미래는 답안지다.** 멸망 이후의 손거울은 "무엇이 10년 뒤까지 살아남는가"를 미리 보여 준다. 미래를 읽을 줄 아는 플레이어가 과거를 올바르게 쓴다.

### 1.3 스코프 선언

방 **하나**, 진입 가능한 시간층 **하나**(10년 전), 보기 전용 시간층 **하나**(멸망 후), 이동 가능한 거울 **하나**, 고정 손거울 **하나**, 퍼즐 **세 개**. 이 문서는 그중 두 번째 퍼즐 「열쇠 릴레이」를 완전한 예시로 명세한다. 첫 번째 퍼즐(투사 학습)과 세 번째 퍼즐(희생 엔딩)은 같은 시스템의 부분집합이므로 별도 명세 없이 구현 가능하다.

---

## 2. 용어 정의

| 용어 | 정의 |
|---|---|
| **본체 (Body)** | 현재 시간층에 존재하는 플레이어. 거울을 옮기고 돌리며, 현재의 물체와 상호작용한다. |
| **분신 (Avatar)** | 빙의 중 과거 시간층을 걷는 반사된 자아. 원뿔 밖으로 나갈 수 없고, 현재로 나올 수 없다. |
| **빙의 (Possession)** | 본체가 거울을 바라보고 진입해 분신으로 전환되는 행위. 빙의 중 본체는 거울 앞에 굳은 조각상으로 남는다. |
| **시간층 (Era)** | 같은 방의 세 가지 시간 단면. `PAST`(10년 전), `PRESENT`(현재), `RUIN`(멸망 후). |
| **원뿔 (Cone)** | 거울면에서 투사 방향으로 뻗는 절두체 형태의 빛 범위. 분신의 존재 가능 영역. |
| **인과 파생 (Causal Derivation)** | 과거의 물체 상태로부터 현재와 폐허의 외형을 계산하는 순방향 규칙. 역방향은 존재하지 않는다. |
| **은닉처 (Cache)** | 물건을 넣으면 10년을 견디는 밀폐 지점. 서랍, 마루장 밑, 벽난로 벽돌의 세 곳. |
| **노출 지점 (Exposed spot)** | 걸이, 맨바닥처럼 물건이 방치되는 곳. 노출된 물건은 10년을 살아남지 못한다. |

---

## 3. 거울의 법 — 규칙 전문 (1인용 확정판)

이 일곱 조항이 게임의 전부다. 구현의 모든 판정은 이 조항 중 하나로 환원되어야 한다.

1. **창의 법.** 거울은 통과하는 문이 아니라 들여다보는 창이다. 각 거울은 고정된 하나의 시간층만 비춘다. 전신 거울은 `PAST`, 손거울은 `RUIN`을 비춘다.
2. **빛의 법.** 분신은 거울이 투사하는 원뿔 안에서만 존재한다. 원뿔 경계를 넘는 이동은 차단된다. 물건 내려놓기는 원뿔 안 어디서나 가능하며, 경계에 막혔을 때 허용되는 행동은 뒤로 물러나기와 내려놓기 두 가지뿐이다.
3. **빙의의 법.** 거울을 바라보고 진입하면 의식이 분신으로 옮겨 간다. 빙의 중 거울은 움직일 수 없다. 따라서 플레이는 언제나 「본체로 조준 → 빙의로 실행 → 복귀 → 재조준」의 왕복이다. 동시에 존재하는 분신은 하나뿐이다.
4. **강의 법.** 인과는 `PAST → PRESENT → RUIN` 방향으로만 흐른다. 과거의 변화는 현재와 폐허에 즉시, 세월을 먹은 모습으로 반영된다. 현재의 행동은 과거에 결코 닿지 않는다.
5. **배달의 법.** 어떤 물체도 거울면을 통과하지 못한다. 과거의 물건을 현재로 옮기는 유일한 경로는 은닉처다. 노출 지점에 방치된 물건은 10년 사이에 사라진다(파생 규칙 R2, 6.7절).
6. **유리의 법.** 과거층 안에서 미래 거울의 원형을 깨면, 그로부터 비롯된 현재의 거울이 소급하여 사라진다. 이 규칙은 최종 퍼즐(희생 엔딩)에서 단 한 번 사용되며, 일반 퍼즐에는 개방하지 않는다.
7. **하나의 방의 법.** 무대는 방 하나와 그 방의 세 시대뿐이다. 진행감은 새 공간이 아니라 같은 방을 다르게 읽게 되는 이해의 심화로 만든다.

---

## 4. 무대: 방과 세 시간층

방은 동서 8m, 남북 6m의 단일 공간이다. 좌표계는 **x축 동(+)/서(−), z축 남(+)/북(−), y축 상(+)**, 원점은 방 중앙 바닥.

```
                    북쪽 벽 (z = -3)
     열쇠 걸이 ◦(-1.0,-3)    잠긴 문 ▣(0,-3)
   ┌──────────────────────────────────────────┐
   │ [서재 책상]                    [벽난로]  │
   │  (-3.2,-1.0)                  (3.7, 0.9)│
   │   서랍 은닉처                  벽돌 은닉처│
   │                                          │
   │      [마루장 은닉처 (-0.8, 1.0)]         │
   │                                          │
   │      ▨ 무너진 대들보 (0,-0.2) ▨          │  ← PRESENT에만 존재
   │                                          │
   │  Ⓐ(-2.5,2.6) ←─ 거울 레일 ─→ Ⓑ(2.5,2.6) │
   └──────────────────────────────────────────┘
        남쪽 벽 (z = +3): 폐허 손거울 ◫(0, +3, 벽걸이)
```

세 시간층은 같은 지오메트리의 세 벌 복제이며 드레싱만 다르다.

| | PAST (10년 전) | PRESENT (현재) | RUIN (멸망 후) |
|---|---|---|---|
| 조명/색 | 따뜻한 주황, 온전한 가구 | 무채색, 먼지, 대들보 붕괴 | 청록, 잔해와 재 |
| 대들보 | 없음 (천장에 붙어 있음) | 방 중앙을 점유, 통행 차단 | 완전 붕괴, 잔해 더미 |
| 책상 | 온전, 서랍 여닫기 가능 | 대들보에 눌려 서랍 찌그러짐 | 잿더미 |
| 마루장 | 온전, 들어올리기 가능 | 뜯긴 채 비어 있음 | 뜯긴 채 비어 있음 |
| 벽난로 벽돌 | 헐거움, 빼고 끼우기 가능 | 온전, 상호작용 가능 | **온전하게 서 있음 (답안지)** |
| 열쇠 걸이 | 열쇠가 걸려 있음 | 비어 있음 (녹 자국만) | 벽째 소실 |

**보행 영역 규정:** `PAST`의 남쪽 레일 지대(z > 2.2)는 낮은 선반과 화분으로 드레싱하여 비보행 처리한다. 거울 바로 옆과 뒤는 원뿔이 물리적으로 닿을 수 없는 사각이므로, 그 지대를 걷지 못하게 막아야 검증 조건 3(6.12절)이 성립한다.

---

## 5. 예시 시나리오 전체 워크스루 — 「열쇠 릴레이」

이 장은 게임을 전혀 모르는 독자를 위해, 퍼즐 하나를 플레이어의 눈으로 처음부터 끝까지 따라간다. 각 단계에 그 순간 작동하는 법 조항을 표기한다. 이 워크스루에 등장하지 않는 규칙은 이 게임에 없다.

### 상황 설정

북쪽의 문은 잠겨 있다. 문 옆 벽의 열쇠 걸이는 현재에는 비어 있고 녹슨 자국만 남아 있다. 그런데 전신 거울을 북쪽으로 돌려 보면, 거울 속 10년 전의 걸이에는 **열쇠가 걸려 있다**.

### 1단계 — 미래라는 답안지를 읽는다 (창의 법)

플레이어가 남쪽 벽의 손거울을 들여다본다. 손거울 속 멸망 후의 방에서: 책상은 잿더미가 되었고, 마루장은 뜯긴 채 비어 있으며, 벽난로 벽면만 헐거운 벽돌이 박힌 채 온전히 서 있다.

> **플레이어가 배우는 것:** 붕괴는 한 방향으로만 진행되므로, "멸망 후에도 온전한 것은 현재에도 온전하다". 물건을 10년 뒤로 배달하려면 살아남을 곳에 넣어야 하고, 그 후보는 벽난로 벽돌뿐이다. 이것이 이 퍼즐의 첫 번째 깨달음(논리)이다.

### 2단계 — 과거를 조준한다 (창의 법, 빛의 법)

플레이어가 전신 거울을 레일의 서쪽 끝 Ⓐ까지 끌고 가서, 북쪽 문과 열쇠 걸이 방향으로 회전시킨다. 거울에서 뻗어 나온 반투명한 빛의 원뿔이 문 앞 구역과 걸이를 덮는다. 바닥에는 원뿔의 경계선이 은은한 링으로 표시된다.

### 3단계 — 빙의하여 열쇠를 집는다 (빙의의 법)

거울 앞에 서서 진입 키를 누른다. 화면이 유리 깨지는 소리 없이 물결치며, 시점이 거울 속으로 넘어간다. 이제 플레이어는 10년 전의 따뜻한 방을 걷는 분신이다. 뒤를 돌아보면 거울 저편에 굳어 있는 본체의 조각상이 보인다. 분신은 걸이로 걸어가 열쇠를 집는다.

### 4단계 — 빛의 경계에서 열쇠를 놓는다 (빛의 법, 배달의 법)

분신이 열쇠를 들고 벽난로 쪽(동쪽)으로 걷는다. 그러나 몇 걸음 못 가 원뿔의 경계에 도달하고, 몸이 더 나아가지 않는다. 경계 링이 밝게 점멸하며, 화면에 "빛이 여기까지만 닿는다"는 짧은 표시가 뜬다. 거울면으로 돌아가 열쇠를 든 채 유리를 빠져나가려 해도, 열쇠가 유리에 **탁** 부딪히는 소리와 함께 손이 막힌다.

> **플레이어가 배우는 것:** 나도 물건도 빛 밖으로 못 나간다. 그러나 물건을 **경계 바닥에 내려놓는 것**은 된다. 분신은 원뿔 동쪽 경계의 바닥에 열쇠를 내려놓고 거울로 복귀한다. 이것이 두 번째 깨달음(조작)이며, 이 게임의 퍼즐 난이도는 이후 이 릴레이 횟수로만 조절된다.

### 5단계 — 원뿔을 옮겨 릴레이를 잇는다 (빙의의 법 왕복)

본체로 돌아온 플레이어가 거울을 레일 동쪽 끝 Ⓑ로 끌고, 방금 열쇠를 놓아 둔 지점과 벽난로가 한 원뿔에 함께 들어오도록 회전시킨다. 다시 빙의한다. 분신이 바닥의 열쇠를 주워 벽난로로 가서, 헐거운 벽돌을 빼고 열쇠를 넣은 뒤 벽돌을 도로 끼운다.

### 6단계 — 미래가 먼저 대답한다 (강의 법)

복귀한 본체가 손거울을 다시 들여다보면, 조금 전까지 온전하던 폐허의 벽돌이 **열린 채 비어 있는** 모습으로 바뀌어 있다. 미래가 비어 있는 이유는, 현재의 내가 지금 꺼낼 것이기 때문이다. 인과가 플레이어보다 한 발 앞서 움직였다는 것을 미래가 증명한다.

### 7단계 — 현재에서 수확한다 (배달의 법)

본체가 현재의 벽난로로 가서 같은 벽돌을 뺀다. 안에서 10년 녹슨 열쇠가 나온다. 북쪽 문에 꽂으면 잠금이 풀리고 퍼즐이 끝난다.

### 실패했다면 — 모든 실패는 가르친다

- **서랍에 숨긴 경우:** 현재로 복귀하면 서랍이 대들보에 눌려 찌그러져 있고, 틈새로 열쇠가 **보이지만 꺼낼 수 없다**. 다시 빙의하면 과거의 열쇠는 서랍 속 그대로이므로 꺼내 재배치하면 된다. 막다른 상태(소프트락)는 구조적으로 불가능하다.
- **마루장 밑에 숨긴 경우:** 현재의 마루장은 이미 뜯겨 있고 공동은 비어 있다. 10년 사이 누군가 가져간 것이다. "폐허에서 부서진 곳은 현재에 이미 부서져 있을 수 있다"는 1단계 논리의 역방향을 가르친다.
- **열쇠를 원뿔 밖으로 던지려는 경우:** 던지기 동작 자체가 없다. 놓기(place/drop)만 존재하며, 놓기는 분신의 발밑 반경 안에서만 성립하므로 회수 불가능 지점은 만들어질 수 없다.

---

## 6. 시스템 명세 (구현 대상)

### 6.1 기술 스택과 배포

- **렌더링:** Three.js (r160 이상), ES Module을 CDN(`unpkg` 또는 `jsdelivr`)에서 import map으로 로드. 번들러와 빌드 단계 없음.
- **배포:** 정적 파일 일체를 GitHub Pages 또는 itch.io HTML 업로드. 서버, 데이터베이스, 계정 없음.
- **저장:** 진행 상태는 세션 한정. 필요 시 `localStorage`에 퍼즐 클리어 플래그만 기록.
- **입력:** `PointerLockControls` 기반 1인칭. 키 바인딩은 6.8절.
- **금지 사항:** 외부 물리 엔진 도입 금지(충돌은 AABB로 충분), 후처리 체인 남용 금지(비네트 1개만 허용).

### 6.2 씬 구조

세 개의 독립 `THREE.Scene`이 **동일한 좌표계**를 공유한다. 방 지오메트리는 한 번 만들어 `clone()`으로 세 벌을 만들고, 시간층별 드레싱(재질, 소품, 조명)만 갈아 끼운다.

```
scenes = {
  PAST:    Scene { roomShell, deskIntact, hookWithKey, fireplaceLoose, boardsIntact, warmLights }
  PRESENT: Scene { roomShell, deskCrushed, beamCollapsed, hookEmpty, fireplaceIntact,
                   standingMirror(era=PAST), handMirror(era=RUIN), bodyMesh, greyLights }
  RUIN:    Scene { roomShell(damaged), ashPile, boardsTorn, fireplaceWall, tealLights }
}
```

- 메인 카메라는 상태에 따라 `PRESENT`(본체 모드) 또는 `PAST`(빙의 모드)를 렌더링한다. `RUIN`은 메인 카메라로 절대 렌더링되지 않는다(창의 법: 손거울은 보기 전용).
- 충돌용 AABB 목록은 시간층별로 별도 보관한다. `PRESENT`에는 대들보 AABB가 있고 `PAST`에는 없다는 차이가 퍼즐의 전제다.

### 6.3 거울 표면 렌더링

- **전신 거울(PAST):** Three.js 예제의 `Reflector`(`three/examples/jsm/objects/Reflector.js`)를 복사하여 `MirrorPortal` 클래스로 개조한다. 개조 지점은 단 하나로, `onBeforeRender` 내부의 `renderer.render(scene, virtualCamera)`에서 렌더 대상 씬을 자기 자신이 속한 씬이 아니라 `scenes[this.era]`로 바꾼다. 반사 가상 카메라의 자세 계산(거울 평면에 대한 카메라 반사)은 원본 코드를 그대로 쓴다. 세 씬이 좌표계를 공유하므로 추가 변환은 불필요하다.
  - 렌더 타깃 해상도 512×512. 카메라 절두체에 거울이 들어올 때만 갱신.
- **손거울(RUIN):** 실시간 반사가 불필요하다. 폐허 씬 안에 고정 가상 카메라 한 대를 두고 렌더 타깃(256×256)에 렌더링하되, `ruinDirty` 플래그가 참일 때만 재렌더링한다. 플래그는 인과 파생 갱신 시(6.7절)에만 켜진다.
- **과거층 속 거울의 원형:** `PAST` 씬에는 전신 거울의 10년 전 원형이 존재해야 하지만(유리의 법의 전제), 반드시 **천으로 덮인 소품**으로 배치한다. 이유는 두 가지다. 첫째, 거울 속 거울의 재귀 렌더링을 원천 차단한다. 둘째, 퍼즐 3 이전에 원형을 깨는 조기 발동을 막는다. 깨기 동사는 퍼즐 3에서 지급되는 도구가 있을 때만 활성화된다.

### 6.4 빙의 전환

상태 기계: `MODE_BODY ⇄ MODE_AVATAR`.

- **진입 조건:** 본체가 전신 거울로부터 3m 이내, 화면 중앙 레이캐스트가 거울면에 명중, 진입 키(F) 입력.
- **진입 절차:**
  1. 0.4초 페이드(물결 셰이더 또는 단순 크로스페이드).
  2. 본체 위치와 자세를 저장하고, `PRESENT` 씬의 `bodyMesh`를 그 자리에 굳은 자세로 표시.
  3. 분신 스폰 위치 = 거울 중심을 바닥에 투영한 점 + 투사 방향으로 0.6m. 시선은 투사 방향.
  4. 메인 렌더 대상 씬을 `PAST`로 전환. 이동/충돌 컨텍스트를 `PAST`의 AABB 목록으로 교체.
  5. 좌우 반전 활성화(6.5절, 기능 플래그).
- **복귀 조건:** 분신이 거울 평면으로부터 1.5m 이내에서 F 입력. 절차는 진입의 역순. 단, `CARRIED` 상태(물건 소지 중)에서는 복귀가 **거부**된다: `glass_tap` 재생 + "물건은 유리를 건널 수 없다" 표시. 배달의 법은 몸의 통과만이 아니라 의식 전환에 얹혀 오는 물건에도 적용된다. 먼저 놓아야(G) 돌아올 수 있다.
- **잠금:** `MODE_AVATAR` 동안 거울 끌기/회전 입력은 무시된다(빙의의 법). `MODE_BODY` 동안 분신은 존재하지 않는다(동시 분신 1명 제한은 상태 기계 자체가 보장).

### 6.5 좌우 반전 (기능 플래그 `MIRROR_FLIP`, 기본 켜짐)

분신은 반사된 존재이므로 과거 세계는 좌우가 뒤집혀 보인다. 게임플레이 판정에는 관여하지 않는 순수 표현 계층이다.

```js
// 빙의 모드의 매 프레임, updateProjectionMatrix() 직후:
camera.projectionMatrix.elements[0] *= -1;   // x축 부호 반전
// 입력 반전: A/D 스왑, 마우스 x 델타 부호 반전
```

- 투영 행렬 반전은 삼각형 감기 방향(winding)을 뒤집으므로, `PAST` 씬의 모든 재질은 `side: THREE.DoubleSide`로 생성한다(로우폴리 씬이므로 비용 무시 가능).
- 과거 벽면의 문자 텍스처가 자동으로 거꾸로 보이게 되며, 이것이 "지금 거울 안"임을 알리는 상시 신호다.
- 구현이 흔들리면 플래그를 끄고 출시해도 게임은 성립한다. 판정 의존이 없기 때문이다.

### 6.6 빛의 원뿔 판정

원뿔은 거울 로컬 공간의 절두체다. 로컬 +Z가 투사 방향, 원점이 거울면 중심.

```js
function insideCone(mirror, worldPos) {
  const p = mirror.mesh.worldToLocal(worldPos.clone());
  if (p.z < 0 || p.z > mirror.coneLength) return false;
  const grow = p.z * Math.tan(mirror.spreadAngle);
  return Math.abs(p.x) <= mirror.halfWidth  + grow
      && Math.abs(p.y) <= mirror.halfHeight + grow;
}
```

- **이동 클램프:** 분신의 이동 후보 위치가 `insideCone == false`이면 이동을 벽 충돌과 동일하게 차단하고, 경계 링 데칼을 0.5초 점멸시킨다.
- **놓기 예외:** 분신이 물건을 든 채 경계에 붙어 있으면 놓기(G)만 허용. 놓인 위치는 분신 발밑(반경 0.5m 이내로 클램프)이므로 정의상 원뿔 안이다. 원뿔 밖에 물건이 존재하는 상태는 시스템상 만들어질 수 없다.
- **시각화:** 원뿔은 반투명 가산 재질의 절두체 메시 + 바닥 경계 링 데칼로 상시 표시한다. 본체 모드에서 거울을 회전하는 동안에도 보여서 조준 도구 역할을 한다.

### 6.7 상태 모델과 인과 파생

퍼즐의 **유일한 진실 원천은 과거층의 물체 상태**이며, 현재와 폐허의 외형은 순수 함수로 파생된다(강의 법의 코드 번역).

```js
const KeyLoc = Object.freeze({
  HOOK: 'HOOK',            // 걸이 (노출)
  FLOOR: 'FLOOR',          // 바닥 좌표 {x, z} (노출)
  DRAWER: 'DRAWER',        // 서랍 (은닉처)
  BOARD: 'BOARD',          // 마루장 밑 (은닉처)
  BRICK: 'BRICK',          // 벽난로 벽돌 (은닉처)
  CARRIED: 'CARRIED',      // 분신이 들고 있음 (PAST에서만 유효)
  RETRIEVED: 'RETRIEVED',  // 현재에서 회수 완료 (종결 상태)
});
let keyLoc = { type: KeyLoc.HOOK };   // 초기 상태
```

**파생 규칙(전체 표):**

| keyLoc (PAST의 사실) | PRESENT 파생 | RUIN 파생 |
|---|---|---|
| HOOK | 걸이 빔 — 녹 자국 데칼만 | 벽째 소실 |
| FLOOR{x,z} | (x,z)에 녹물 얼룩 데칼, 열쇠 없음 | 잔해에 묻힘, 표시 없음 |
| DRAWER | 찌그러진 서랍 틈새로 열쇠 보임, 회수 불가, 조사 시 "대들보가 서랍을 눌렀다" | 잿더미 |
| BOARD | 뜯긴 마루장, 공동 비어 있음, 조사 시 "누군가 먼저 가져갔다" | 뜯긴 채 비어 있음 |
| BRICK | 온전한 벽돌, 상호작용 → 회수 가능 | **열린 채 비어 있음** (6단계 연출) |
| RETRIEVED | 열쇠가 본체 인벤토리, 문 잠금 해제 가능 | 변화 없음 |

- **파생 규칙 R1 (은닉):** 은닉처의 물건만 PRESENT에 물리적으로 도달한다.
- **파생 규칙 R2 (노출 소실):** 노출 지점(HOOK, FLOOR)의 물건은 PRESENT에 흔적 데칼만 남긴다. 이 규칙이 "현재의 걸이에서 그냥 열쇠를 집는" 자명한 해법을 봉쇄한다.
- **갱신 절차:** `keyLoc`이 바뀔 때마다 `applyDerivation()`을 호출하여 PRESENT와 RUIN 씬의 해당 소품 표시 상태만 교체하고 `ruinDirty = true`로 둔다. 프레임 단위 폴링 없음, 이벤트 구동.
- **종결 처리 (역설 봉인):** PRESENT에서 벽돌 상호작용으로 `RETRIEVED` 전이가 일어나면, 빙의를 영구 비활성화하지 **않는다**(퍼즐 3이 빙의를 전제하므로). 대신 열쇠 객체만 시간선에서 봉인한다: `PAST`의 벽돌 속 열쇠는 계속 보이지만 상호작용 시 "이미 일어난 일이다"라는 문구와 함께 거부된다. 회수된 열쇠를 과거에서 다시 옮겨 파생 상태를 모순시키는 경로는 이 봉인 하나로 닫힌다. 문 열림 시퀀스 동안만 일시적으로 빙의 입력을 잠근다.

### 6.8 상호작용 시스템

- **판정:** 화면 중앙 레이캐스트, 사거리 2.5m. 명중한 상호작용 대상은 외곽선 하이라이트 + 한 단어 프롬프트("집기", "넣기", "빼기", "들여다보기").
- **키 바인딩:** WASD 이동, 마우스 시점, `E` 상호작용(집기/넣기/빼기/조사), `G` 놓기(분신 전용), `F` 빙의/복귀, 거울 끌기 = 거울에 `E` 홀드 후 마우스 좌우(레일 이동)와 휠(요 회전).
- **거울 이동 제약:** 위치는 레일 선분 Ⓐ–Ⓑ 위로 클램프, 회전은 요(yaw) 단일 축, 레벨 데이터의 `yawRange` 안으로 클램프. 이동과 회전 중 원뿔 시각화가 실시간 갱신된다.
- **인벤토리:** 없음. 손은 하나, 들 수 있는 물건도 한 번에 하나(`CARRIED`). UI 부담과 배달의 법 우회를 동시에 제거한다.

### 6.9 실패 상태 명세 (각각 반드시 구현)

| 시도 | 시스템 응답 | 가르치는 법 |
|---|---|---|
| 열쇠 든 채 거울면 통과 | 이동 차단 + 유리에 부딪히는 "탁" 효과음 + 유리면 파문 셰이더 1회 | 배달의 법 |
| 열쇠 든 채 원뿔 경계 돌파 | 이동 클램프 + 경계 링 점멸 + "빛이 여기까지만 닿는다" 표시 + G 프롬프트 | 빛의 법 |
| 서랍 은닉 후 현재에서 회수 시도 | 조사 텍스트와 틈새의 열쇠 렌더링, 회수 입력 없음 | 인과 파생 R1의 한계 학습 |
| 마루장 은닉 후 현재에서 회수 시도 | 빈 공동 + 조사 텍스트 | 폐허 답안지의 역방향 논리 |
| 빙의 중 거울 조작 시도 | 입력 무시 (프롬프트 자체가 뜨지 않음) | 빙의의 법 |
| 열쇠 든 채 F로 복귀 시도 | 복귀 거부 + `glass_tap` + "물건은 유리를 건널 수 없다" | 배달의 법 |
| RETRIEVED 후 과거의 벽돌 속 열쇠 상호작용 | 거부 + "이미 일어난 일이다" | 강의 법 (역설 봉인) |

### 6.10 오디오 훅 (필수 6종)

`possess_in`(물결), `possess_out`, `glass_tap`(배달의 법 차단), `boundary_hum`(경계 접근 시 저음 상승), `brick_scrape`, `door_unlock`. 배경은 시간층별 앰비언스 2종(PAST 난롯불, PRESENT 바람)을 빙의 전환 페이드에 맞춰 크로스페이드. 무음 빌드 제출 금지.

### 6.11 레벨 데이터 포맷 (`level.json`)

```json
{
  "room": { "w": 8, "d": 6 },
  "mirror": {
    "rail": [[-2.5, 2.6], [2.5, 2.6]],
    "yawRangeDeg": [150, 390],
    "halfWidth": 0.5, "halfHeight": 0.9,
    "coneLength": 6.2, "spreadAngleDeg": 15
  },
  "handMirror": { "pos": [0, 1.5, 3.0] },
  "props": {
    "door":      { "pos": [0, 0, -3] },
    "hook":      { "pos": [-1.0, 1.4, -3] },
    "desk":      { "pos": [-3.2, 0, -1.0] },
    "board":     { "pos": [-0.8, 0, 1.0] },
    "fireplace": { "pos": [3.7, 0, 0.9] },
    "beamAABB":  { "min": [-1.6, 0, -1.1], "max": [1.6, 2.2, 0.7], "era": "PRESENT" }
  },
  "tuning_note": "spreadAngleDeg와 rail 끝점은 6.12 검증을 통과하도록 조정 가능한 값이다."
}
```

### 6.12 검증 스크립트 (`validate.js`, 구현 필수)

레벨 수치는 조정 가능하되, 다음 네 조건을 자동 검사로 보장해야 한다. 조건 0이 퍼즐의 성립 자체를 지키는 최우선 검사다. 실행은 개발 중 콘솔 호출로 충분하다. 보행 가능 영역의 정의는 4장의 규정(z ≤ 2.2)을 따른다.

0. **단일 자세 불가능성 (치팅 봉쇄):** 레일 위치와 요 각의 전 조합(격자 탐색: 위치 0.05m, 각도 0.1°)에 대해, 걸이와 벽난로를 **동시에** 포함하는 원뿔이 존재하지 않아야 한다. 이 조건이 깨지면 한 번의 빙의로 열쇠를 옮길 수 있어 릴레이 구조 전체가 붕괴한다. (v1.0 수치는 이 검사에서 10,882개 자세가 적발되어 v1.1에서 걸이 위치와 원뿔 수치를 조정했다.)
1. **걸이 도달 성립:** 걸이를 포함하는 합법 자세가 하나 이상 존재한다.
2. **릴레이 성립:** 걸이를 포함하는 어떤 자세의 원뿔과 벽난로를 포함하는 어떤 자세의 원뿔이, 보행 가능 바닥에서 공유 셀을 갖는다. 이 공유 셀이 열쇠를 내려놓는 릴레이 지점이다. (같은 레일 위치에서 회전만 바꾼 자세 쌍도 허용된다 — 7.1절 참조.)
3. **회수 불능 지점 부재:** 보행 가능 바닥을 0.25m 격자로 샘플링했을 때, 모든 셀이 「어떤 합법적 거울 자세의 원뿔」 합집합에 포함된다. (놓기 클램프 규칙과 함께 이중 방어.)

### 6.13 성능 예산

- 목표: 통합 그래픽 칩, 1080p, 60fps.
- 렌더 패스: 메인 1 + 전신 거울 RT 1(가시 시에만) + 손거울 RT(더티 시에만) = 통상 2패스.
- 드로 콜 150 이하, 총 자산 5MB 이하(지오메트리는 프리미티브 조합, 텍스처는 색상 팔레트와 데칼 소수).
- 그림자 맵 금지. 조명은 시간층당 방향광 1 + 앰비언트 1, 벽난로만 포인트광 1.

---

## 7. 수용 기준 (Acceptance Criteria)

### 7.1 해법 유일성 선언

이 퍼즐의 유일성은 **전략 수준**에서 보장되고 **실행 수준**에서는 의도적으로 복수다.

- **전략 수준(유일):** (a) 최종 은닉처는 벽돌뿐이다 — 파생 표가 나머지 후보를 전부 실패시킨다. (b) 걸이에서 벽돌까지는 최소 두 번의 빙의와 그 사이의 재조준이 필요하다 — 검증 조건 0이 이를 기하학적으로 강제한다.
- **실행 수준(복수, 허용):** 릴레이 지점의 좌표, 레일을 끄는지 회전만 하는지, 시행착오로 은닉처를 소거하는지 손거울 답안지를 먼저 읽는지는 모두 자유다. 특히 같은 레일 위치에서 회전만으로 잇는 릴레이는 유효한 해법으로 **수용**한다. 벽난로가 레일에 가까워 어느 레일 위치에서도 포함 가능하다는 기하학적 사실 때문에 이를 막으려면 원뿔 차폐 같은 새 규칙이 필요한데, 핵심 루프(빙의 왕복 + 재조준)가 이미 보존되므로 규칙 추가 비용을 지불하지 않는다.
- **답안지의 지위:** 손거울 없이도 세 은닉처의 실패가 전부 복구 가능하므로 소거법으로 해에 도달할 수 있다. 답안지는 필수 정보가 아니라 시행 횟수를 줄여 주는 보상이다.

### 7.2 검사 항목

구현 완료의 정의. 각 항목은 독립적으로 검사 가능해야 한다.

1. 본체가 이동하는 동안 전신 거울에 과거층이 올바른 원근으로 실시간 반사된다.
2. 거울 3m 밖, 또는 시선이 거울면을 벗어난 상태에서는 F를 눌러도 빙의되지 않는다.
3. 빙의 중 뒤를 돌면 거울 저편에 굳은 본체 조각상이 보인다.
4. 분신이 열쇠를 들고 거울면에 접근하면 통과가 차단되고 `glass_tap`이 재생된다.
5. 분신의 이동은 원뿔 내부로 클램프되며, 경계에서 G로 놓기가 가능하고 놓인 물건은 항상 원뿔 안에 있다.
6. 서랍/마루장/벽돌 세 은닉처 각각에 대해 6.7절 파생 표와 정확히 일치하는 PRESENT/RUIN 외형이 나타난다.
7. 벽돌 은닉 직후 손거울을 보면 폐허의 벽돌이 열린 채 빈 모습으로 갱신되어 있다.
8. PRESENT 벽돌에서 회수하면 녹슨 열쇠 모델이 인벤토리에 들어오고 문이 열리며, 이후 빙의가 비활성화된다.
9. `validate.js`의 네 검사(조건 0~3)가 현재 `level.json` 수치로 통과한다.
10. 통합 그래픽 칩에서 60fps를 유지한다(6.13 예산 내).

---

## 8. 파일 구조 제안

```
index.html
src/
  main.js          // 부트스트랩, 게임 루프, 모드 상태 기계
  scenes.js        // 세 시간층 씬 구성과 드레싱
  mirror.js        // MirrorPortal (Reflector 개조), 레일 끌기/회전
  possession.js    // 빙의 전환, 좌우 반전 플래그
  cone.js          // insideCone, 이동 클램프, 원뿔 시각화
  causal.js        // KeyLoc 상태 기계, applyDerivation, ruinDirty
  interact.js      // 레이캐스트, 프롬프트, 키 바인딩
  audio.js         // 6.10의 훅 재생과 앰비언스 크로스페이드
  validate.js      // 6.12 검증
  level.json
assets/            // 팔레트 텍스처, 데칼, 효과음
```

## 9. 데모 영상 콘티 (약 40초, 이 퍼즐 분량)

편집 효과 없이 여섯 컷이 네 개의 법을 전부 시연한다: ① 거울 속 걸이의 열쇠 → ② 경계에서 열쇠를 내려놓는 손 → ③ 거울을 Ⓑ로 끄는 본체와 따라 도는 원뿔 → ④ 벽돌에 들어가는 열쇠 → ⑤ 손거울 속, 열린 채 빈 폐허의 벽돌 → ⑥ 현재의 벽돌에서 나오는 녹슨 열쇠와 열리는 문.

## 10. 인접 선례 공개 목록 (README에 명시할 것)

- **Viewfinder** — 투사된 이미지가 현실 공간이 되는 발상.
- **The Art of Reflection** — 거울을 반전 차원으로의 포털로 쓰는 1인칭 퍼즐.
- **Shady Part of Me / In My Shadow** — 투사 영역 안에서만 움직일 수 있는 분신의 골격.
- **Day of the Tentacle / The Past Within** — 과거에서 현재로의 순방향 인과와 시간을 통한 물건 전달.

차별점 선언: 위 선례들은 투사·포털·순방향 인과를 각각 따로 쓴다. 《거울빛》의 고유성은 결합에 있다 — **빛이 닿는 곳만 과거가 존재하고(2법), 물건은 시간이 배달하며(5법), 미래가 답안지로 먼저 대답한다(강의 법의 RUIN 파생)**.
